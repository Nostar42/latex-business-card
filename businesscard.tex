\documentclass[10pt]{article}

\usepackage[paperwidth=3.5in, paperheight=2in, margin=0in]{geometry}
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{qrcode}
\usepackage{hyperref}

\hypersetup{hidelinks}

% --- Fonts (matching website) ---
\setmainfont{DM Sans}[
  BoldFont = DM Sans Bold,
  ItalicFont = DM Sans Italic
]
\newfontfamily\monofont{DM Mono}

% --- Colors ---
\definecolor{cardBg}{HTML}{1a1d27}
\definecolor{cardText}{HTML}{f1f5f9}
\definecolor{cardMuted}{HTML}{6b7280}
\definecolor{cardAccent}{HTML}{6366f1}

\pagestyle{empty}
\setlength{\parindent}{0pt}

% ================================================================
\begin{document}

\begin{tikzpicture}[remember picture, overlay]

  % Background
  \fill[cardBg] (current page.south west) rectangle (current page.north east);

  % Accent bar on left edge
  \fill[cardAccent] (current page.south west) rectangle ([xshift=4pt]current page.north west);

  % --- LEFT SIDE: Name & Info ---
  % Name
  \node[anchor=north west, text=cardText, font=\fontsize{13}{15}\bfseries\selectfont]
    at ([xshift=18pt, yshift=-18pt]current page.north west)
    {CAMERON J. PASTORIUS};

  % Title
  \node[anchor=north west, text=cardAccent, font=\fontsize{7}{9}\monofont\selectfont]
    at ([xshift=18pt, yshift=-34pt]current page.north west)
    {ELECTRICAL DESIGNER · E.I.};

  % Divider line
  \draw[cardMuted, line width=0.3pt]
    ([xshift=18pt, yshift=-42pt]current page.north west) --
    ([xshift=2.2in, yshift=-42pt]current page.north west);

  % Email
  \node[anchor=north west, text=cardMuted, font=\fontsize{6.5}{9}\monofont\selectfont]
    at ([xshift=18pt, yshift=-50pt]current page.north west)
    {cam.pastorius0815@gmail.com};

  % Phone
  \node[anchor=north west, text=cardMuted, font=\fontsize{6.5}{9}\monofont\selectfont]
    at ([xshift=18pt, yshift=-61pt]current page.north west)
    {+1 850-240-8867};

  % LinkedIn
  \node[anchor=north west, text=cardMuted, font=\fontsize{6.5}{9}\monofont\selectfont]
    at ([xshift=18pt, yshift=-72pt]current page.north west)
    {linkedin.com/in/cameron-pastorius};

  % GitHub
  \node[anchor=north west, text=cardMuted, font=\fontsize{6.5}{9}\monofont\selectfont]
    at ([xshift=18pt, yshift=-83pt]current page.north west)
    {github.com/Nostar42};

  % HG Engineers
  \node[anchor=south west, text=cardMuted, font=\fontsize{6}{8}\monofont\selectfont]
    at ([xshift=18pt, yshift=14pt]current page.south west)
    {HG ENGINEERS};

  % --- RIGHT SIDE: QR Code ---
  \node[anchor=east] at ([xshift=-14pt, yshift=0pt]current page.east) {
    \color{white}
    \qrcode[height=1.2in, level=M]{https://github.com/Nostar42}
  };

  % "Scan me" label under QR
  \node[anchor=north, text=cardMuted, font=\fontsize{5.5}{7}\monofont\selectfont]
    at ([xshift=-46pt, yshift=-54pt]current page.north east)
    {SCAN ME};

\end{tikzpicture}

\end{document}
